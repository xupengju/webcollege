var urlT=""
if($("#editor1").attr("id")!=undefined) {
    var ue1 = UE.getEditor('editor1',{
        toolbars: [
            ['source', 'undo', 'redo', 'bold','italic','underline','strikethrough',
                'forecolor','lineheight','paragraph','fontfamily','fontsize','justifyleft','justifyright',
                'justifycenter','justifyjustify','simpleupload','unlink','link']
        ],
        initialFrameWidth:1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight:300,  //初始化编辑器高度,默认320
    });
}
if($("#editor2").attr("id")!=undefined) {
    var ue2 = UE.getEditor('editor2', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor3").attr("id")!=undefined) {
    var ue3 = UE.getEditor('editor3', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}

if($("#editor4").attr("id")!=undefined) {

    var ue4 = UE.getEditor('editor4', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor5").attr("id")!=undefined) {
    var ue5 = UE.getEditor('editor5', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor6").attr("id")!=undefined) {
    var ue6 = UE.getEditor('editor6', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor7").attr("id")!=undefined) {
    var ue7 = UE.getEditor('editor7', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor8").attr("id")!=undefined) {
    var ue8 = UE.getEditor('editor8', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor9").attr("id")!=undefined) {
    var ue9 = UE.getEditor('editor9', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor10").attr("id")!=undefined) {
    var ue10 = UE.getEditor('editor10', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}
if($("#editor11").attr("id")!=undefined) {
    var ue11 = UE.getEditor('editor11', {
        toolbars: [
            ['source', 'undo', 'redo', 'bold', 'italic', 'underline', 'strikethrough',
                'forecolor', 'lineheight', 'paragraph', 'fontfamily', 'fontsize', 'justifyleft', 'justifyright',
                'justifycenter', 'justifyjustify', 'simpleupload', 'unlink', 'link']
        ],
        initialFrameWidth: 1000,  //初始化编辑器宽度,默认1000
        initialFrameHeight: 300,  //初始化编辑器高度,默认320

    });
}